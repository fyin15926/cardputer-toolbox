# Cardputer-Adv 工具箱 项目备忘

## 2026-05-25 机内轻量音质优化说明

这轮写进固件的是轻量实时处理，不是神经网络降噪，也不是网上大模型算法。目标是让 Cardputer 这颗小麦克风录出来的人声更稳、更少刺耳高频和底噪，同时不能让 ESP32-S3 的录音链路卡顿。

依据：
- Cardputer-Adv 没有 PSRAM，不能上 Espressif ESP-SR 这类较重的专业降噪链路。
- 之前样本显示，按键/手指摩擦声通常 RMS 不一定高，但相邻采样变化量大、高频比例高；人声 RMS 更稳定，频谱重心相对低。
- 用户主要需求是清晰语音、可在机器本机播放、并能提升语音转文字；所以原始 WAV 保留，机内只做保守轻处理，服务器另做 ASR-clean。

固件当前 `processMicBuffer()` 思路：
- 先估计本帧 RMS 和相邻采样平均差分 `avgDiff`。
- 用 `rms < 520 && avgDiff > 120 && avgDiff/rms 较高` 判定疑似摩擦/细碎高频噪声。
- 维护一个很慢更新的 `noiseRms`，只在安静且不像摩擦的帧里学习底噪。
- 对接近底噪的帧做轻微 soft gate，不硬切静音，避免人声一卡一卡。
- 每个样本做 DC/high-pass 去偏移，再走一阶低通 `lpfCoef`。
- 疑似摩擦时减少高频混合 `hiMixQ8`，正常人声尽量保留高频清晰度。
- 最后用 `tanhf` 做软限幅，防止大声或碰撞时爆裂。

当前取舍：
- 优点：实时、轻量、不需要新库，录音不容易卡；比强门限/强 AGC 更自然。
- 缺点：不是专业降噪，环境噪声很大时只能改善，不能“抹干净”；参数过强会让人声发闷或断续。
- 下一步如果继续优化，优先收集三类样本：安静人声、有摩擦的人声、环境音。不要只凭单条录音盲调。

## 2026-05-25 服务器 ASR-clean 自适应门限版

服务器端 `buildAsrCleanWav()` 已从固定噪声门升级为“自适应底噪 + 平滑开关门”：
- 先扫描整条录音的帧 RMS，取低 20% 分位估计 `noiseFloorRms`。
- 实际识别门限 `adaptiveGateRms = max(手动 noiseGateRms, noiseFloorRms * 2.25 + 80)`，上限 1800，避免环境底噪变高时固定门限失效。
- 帧级 gate gain 先平滑：开门快、关门慢，再做一个轻微反向前视，保护字头。
- 样本级 gate gain 继续平滑，避免门限跳变造成“咔哒”或字尾被切。
- metadata 新增 `noiseFloorRms`、`adaptiveGateRms`、`minGateGain`，方便后台看这条录音到底被门限处理了多少。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260525-adaptive-asr-clean`。这版只影响服务器生成的 `clean-for-asr.wav` 和后续 DashScope 转写，不影响机器端原始录音。

## 2026-05-25 REC_0007 实测结论

测试录音：`20260525_225500_cardputer-001_REC_0007.wav`，时长约 45.38 秒。原始音频 DashScope 识别整体可用，错误主要集中在最后一句；ASR-clean 版反而把“请记录”识别成“请借入”，最后一句也更偏，所以这条样本最终已切回 `asrSource=raw`。

本次 ASR-clean 指标：`noiseFloorRms=5392`、`adaptiveGateRms=1800`、`gatedFrames=0/2269`。含义是这条录音整体环境/人声能量都很高，自适应门限没有真正开门降噪，后面的高通、预加重、压缩和归一化可能改变了语音特征，导致识别不如原始版。结论：服务器默认仍应保留 raw 优先；ASR-clean 只作为对比实验，不要自动替代最终转写，除非后续多条样本证明它稳定更准。

后续优化方向：先收集三类样本（安静人声、普通环境人声、真实环境音），每条都同时比较 raw 与 ASR-clean 的转写错误；如果要继续优化 ASR-clean，优先做“轻预处理/不压缩”的版本，而不是继续加大降噪强度。

## 2026-05-25 服务器 raw 优先 + ASR 对比版

已部署服务器策略更新：自动上传/默认重跑时，最终转写优先使用原始音频 `raw`；服务器仍会生成 `clean-for-asr.wav`，但默认只作为后台试听、A/B 对比和调试文件，不自动覆盖最终转写。后台页面已把按钮改成“识别对比版/实验：用识别版转写”，避免误以为 ASR-clean 一定更好。

原因：REC_0007 实测证明，当前 ASR-clean 在高能量录音里可能不降噪反而改变语音特征，导致 DashScope 识别变差。后续如果要让系统自动选更优版本，应先做多样本对照和明确的错误率评估，再启用自动切换。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260525-raw-first-asr-compare`。

## 2026-05-25 外语翻译规则修正

问题：REC_0008 是英文 F1 评论片段，DeepSeek 只做了中文摘要，`corrected_text` 仍原样保留英文 ASR，所以 flomo 的“原文”区没有中文翻译。

修正：已强化 DeepSeek 设置和服务端默认提示词。以后如果 ASR 原始文本主要是英文、日文或其他外语，`corrected_text` 必须先给中文翻译；如果原始识别很碎，要标注“部分语句疑似识别不清”，并可附原文转写用于核对。REC_0008 已仅重跑 DeepSeek 后处理，未重发 flomo，避免重复 memo。

注意：翻译质量取决于第一步 DashScope 对外语的转写质量。当前 `paraformer-v2` 对这段英文 F1 广播识别很破碎，所以翻译只能基于可确认文本，不可能完全恢复原视频台词。后续如果常录外语，应单独优化 ASR 模型/语言策略。

部署备份：服务器 `/opt/cardputer-voice/deepseek-settings.json.bak-20260525-foreign-translation`、`/opt/cardputer-voice/server.js.bak-20260525-foreign-translation-prompt`。

## 2026-05-25 说话人分离功能

已接入 DashScope/Paraformer 的 `speaker_count` 参数。后台“转写设置”页新增“说话人数量”：默认“不分角色”，录对话/采访/会议时可选 2、3、4 人后重跑完整转写。服务器会把 DashScope 返回的 `speaker_id` 解析成：

```text
说话人1：……
说话人2：……
```

并把这份分角色文本作为后续 DeepSeek 整理和 flomo memo 的输入。DeepSeek 运行时提示词也已更新：如果 ASR 文本包含“说话人1/说话人2”等标签，必须保留标签和对话顺序，不要合并成一段。

注意：这是“说话人分离/diarization”，不是实名识别。它只能标成说话人1、说话人2，不能知道谁是具体某个人；单麦克风、多人同时说话、电视外放、噪声大时会不稳定。普通单人备忘不要开启，以免被误分。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260525-speaker-diarization`、`/opt/cardputer-voice/deepseek-settings.json.bak-20260525-speaker-diarization`。

## 2026-05-25 说话人口令自动触发

已实现“开头或结尾说口令，服务器自动二次重跑分角色转写”。流程：第一次上传仍按默认 raw 转写；服务器扫描转写文本开头 500 字和结尾 500 字，如果检测到人数口令，且当前还没分角色，就自动设置 `asrSpeakerCountRequested` 并重跑一次 DashScope，最终 memo 使用分角色文本。用 `autoSpeakerRetryDone` 防止重复循环。

支持的常用说法：
- “这是两人对话”
- “以上是两人对话”
- “按两个人分角色”
- “请区分两个说话人”
- “这是三人会议”
- “按三个人分角色”
- “请区分三位说话人”
- “不分角色 / 单人备忘”会明确不触发。

后台会显示“已按录音口令自动启用 N 人分角色”。注意：口令需要被第一次 ASR 识别出来才会触发；如果口令在噪声里没识别准，仍可去后台手动选择人数重跑。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260525-speaker-command`。

## 2026-05-26 分角色文本分段优化

问题：REC_0011 已经生成“说话人1/说话人2”，但 DeepSeek 返回的 `corrected_text` 把所有轮次压成一行，flomo 阅读困难。

修正：服务端新增 `formatSpeakerDialogueText()` 作为 memo 生成层兜底格式化。无论 DeepSeek 是否主动换行，只要文本里出现 `说话人N：` 或 `Speaker N:`，都会在每个说话人标签前插入换行，并清理多余空行。这样分角色原文会输出为：

```text
说话人1：……
说话人2：……
说话人1：……
```

REC_0011 已仅重跑 DeepSeek 后处理并验证格式正常；未重发 flomo，避免重复 memo。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-speaker-paragraphs`。

## 2026-05-26 说话人自动命名

已实现稳态自动命名：在分角色文本中，如果某个说话人自己的行明确出现“我是X / 我叫X / 我的名字叫X / 这里是X”，服务端会建立安全映射，把后续 `说话人1：` 替换为 `X：`。为了避免误标，别名必须较短、只含中文/英文/数字/点/下划线/短横线，且不能是“我们/你们/测试/对话”等普通词。

示例推荐录法：

```text
这是两人对话。
我是A。
我是B。
```

最好由每个人自己说自己的名字，并稍微停顿。若 ASR 把“我是A我是B”粘到同一个说话人行里，系统不会强行猜测映射，仍保留 `说话人1/说话人2`，避免把人名标错。REC_0011 已验证：该条声明粘连，所以未自动改名，符合安全策略。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-speaker-alias`。

## 2026-05-26 命名说话人标签规范化

问题：REC_0012 中 DeepSeek 输出了 `说话人A：...说话人B：...`，服务端旧格式化只匹配 `说话人1/2`，导致没有自动换行，也没有规范成用户期望的 `A：/B：`。

修正：`formatSpeakerDialogueText()` 已扩展匹配 `说话人A`、`说话人小王`、`Speaker A` 等命名标签；`simplifyNamedSpeakerLabels()` 会把安全别名格式从 `说话人A：` 规范为 `A：`。自我介绍识别也扩展为允许句中出现“我是A”，例如“这是两人对话，我是A。”，以及“你说我是B。”这类 ASR 常见表达。

REC_0012 已验证输出：

```text
A：这是两人对话，我是A。
B：B，你就是这么给我搞事情？咋了？来句英文。
A：我都不知道，我都不知道他能不能识别到。
B：你说我是B，美丽的B，好吧。
```

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-speaker-named-labels`。

## 2026-05-26 REC_0014 角色错位处理

REC_0014 暴露了一个边界：ASR 原始文本本身没有 `speaker_id` 分段，只是一整段；DeepSeek 尝试根据上下文补角色时，会出现“自我介绍粘连”和“称呼被误当成说话人”的情况。服务端已加强提示词，说明：
- `我是A，我是B` 这类粘连声明要拆成两行。
- `说话人A：` 应规范为 `A：`。
- 以另一个参与者名字开头的问句通常是称呼，不代表说话人就是那个人。

同时收紧了服务端兜底纠偏，避免过度正则把“大宝贝”拆成“大/宝/贝”或乱换说话人。REC_0014 已按用户提供的正确版本手动写回 memo 和 polished 文本。结论：这类语义级角色错位不能完全靠固定规则安全解决，后续更适合做“人工校正样本/反馈集”，让 DeepSeek 参考示例，而不是让服务端硬猜。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-dialogue-role-repair`。

## 2026-05-26 待办提取和长独白分段优化

问题：REC_0025 是关于“AI 让产品迭代更灵活”的观点型独白，但 DeepSeek 把普通陈述硬塞进 `待办`，例如“以前要做一个产品……”和“因为一旦你设计好了……”。这类不是行动项，会让 memo 变脏。

修正：
- DeepSeek 提示词新增规则：`todos` 只放明确行动项，例如“要做/记得/下次/待办/提醒/需要去/准备/检查/联系/购买/发送/整理”；观点、解释、经历、判断和感受必须进入 `ideas` 或摘要，不进入待办。
- 服务端新增 `cleanTodoList()` / `isActionableTodo()` 兜底过滤，防止模型仍然把陈述句放进待办。
- 新增 `applyCommonTranscriptRepairs()` 修正常见同音错词，例如“考得那么全面”→“考虑得那么全面”、“当多少职人”→“担多少责任”、“前端后段”→“前端后端”。
- 新增 `ensureReadableParagraphs()`：长独白如果 DeepSeek 没有分段，服务端会按句子自动拆成 2 到 4 段，提升阅读性。

REC_0025 已重跑验证：`待办` 被移除，只保留摘要和想法；原文按“传统开发成本 / AI 快速迭代”拆成两段。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-todo-cleanup`、`/opt/cardputer-voice/server.js.bak-20260526-todo-cleanup-v2`，以及对应 `deepseek-settings.json.bak-20260526-todo-cleanup*`。

## 2026-05-26 低频风扇/轰鸣试听版 v3

问题：用户反馈听感里仍有明显“电脑风扇/环境低沉声”。这类不是普通高频底噪，也不是按键刮擦声；旧的 `Alt` 离线降噪是 FFT 谱减，主要对宽频稳定噪声有效，对持续低频轰鸣不一定明显。固件实时链路目前主要做摩擦检测、软门限和轻低通，低频削减不强，所以风扇/空调/桌面震动容易保留。

已先在服务器试听版实现 `smooth-play-preview-v3`，不直接改固件。新增参数 `rumbleHighpass`：
- 默认 `0.965`：适中削低频轰鸣。
- 人声模式 `0.955`：削得更多，语音更薄但风扇声更少。
- 环境模式 `0.985`：保留现场低频，适合录环境音。

注意：低频削减是全程高通处理，不计入旧指标 `noiseFrames`，所以看到 `noiseFrames=0` 不代表没处理低频。后台旧试听版 v2 会被判为旧版本，需要重新生成本机试听版。

下一步：让用户在后台 A/B 听 raw 和 v3 试听版。如果电脑/网页试听 v3 明显改善，再把轻量高通搬进固件录音或播放链路；如果只有小机器喇叭差，则优先优化播放 EQ/限幅，不动录音原始文件。

## 2026-05-26 试听清理三档 v4

用户反馈网页试听版和原始录音差异不明显，因此先不动固件，把服务器试听版改成更容易 A/B 判断的三档：`轻度清理`、`中度清理`、`强力清理`。三档主要拉开 `rumbleHighpass`、`noiseMix`、`noiseRmsMax` 和高频保留参数，用来判断电脑风扇、空调、桌面震动这类低频环境声到底能不能通过轻量算法改善。

默认 `中度清理` 比 v3 更主动削低频：`rumbleHighpass=0.955`、`gain=1.03`、`noiseMix=0.72`。`强力清理` 用 `rumbleHighpass=0.92` 和更低的 `noiseMix=0.48`，目标是先听到明确差异；如果人声变薄或发闷，再退回 `轻度清理`。算法标记升级为 `smooth-play-preview-v4`，旧 v3 试听文件会被页面忽略，需要重新生成。

注意：这仍是服务器网页试听/调参方案，不会覆盖原始录音，也还没有烧进 Cardputer。只有当网页 v4 的某一档确实有效，再把对应的轻量高通和限幅策略迁移到机器端录音或播放链路。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-preview-v4-presets`。

## 2026-05-26 后台试听交互精简

用户反馈后台试听区按钮太多，无法确认当前到底在听原版还是试听版。已把主试听区改成两个大按钮：`听原版` 和 `听试听版`。点击任一按钮会自动在同一时间点切换播放；再次点击当前播放按钮会暂停。原始音频和试听音频仍保留为隐藏播放器，不再把“加载全部、听识别、同位置切换”等调试按钮放在主路径上。

`听试听版` 会先尝试加载已生成的 v4 试听；如果没有新版试听，会自动按当前中度清理参数生成并播放。高级参数和 ASR 识别版仍保留在下方高级区域，用于后续排查，不作为日常试听入口。

部署备份：服务器 `/opt/cardputer-voice/server.js.bak-20260526-two-button-listen`。

## 2026-05-26 试听清理 v5：底噪判断改到高通后

用户实听反馈 v4 仍然听不出区别。复查 REC_0026 指标后发现，原始 WAV 有明显直流/低频偏移：整段帧 RMS 分位数都在 5000 以上，导致旧版按原始波形判断 `noiseFrames=0`，也就是底噪门完全没触发；v4 实际主要只是在移除低频/DC，而这部分在网页和小喇叭上不一定可听。

修正：`smooth-play-preview-v5` 先对音频做与输出一致的高通/去偏移，再基于高通后的帧 RMS 和 diff 判断底噪与摩擦。metadata 新增 `noiseFloorRms`、`adaptiveNoiseRms`、`analysis=post-highpass`。默认中度清理会在高通后用自适应门限处理安静段/环境底噪，而不是被原始 DC 偏移骗过。

结论：v5 才是值得继续听感比较的版本。若 v5 仍无听感改善，则主要矛盾很可能不是低频轰鸣，而是播放链路、麦克风本体噪声、房间声/风扇宽频噪声，后续应转向机器端播放 EQ 或更强的离线降噪方案。

## 2026-05-26 试听清理 v6：加入可感知人声带宽整形

用户继续反馈 v5 在网页 A/B 里仍听不出区别。由于 v5 已确认处理了约四成帧，但听感仍无变化，说明只移除 DC/低频和轻度安静段衰减不足以处理用户实际听到的噪声。

v6 在 v5 的自适应底噪门后增加一层全局人声带宽整形：保留主体人声，压低一部分高频毛刺/沙沙声，让试听版在普通网页播放设备上也更容易听出差异。metadata 标记 `voiceBandMix=0.58`，算法名 `smooth-play-preview-v6`。

注意：v6 仍只用于网页“人耳试听版”，不覆盖原始录音，也不用于默认 ASR 转写。它的目标是先验证“可听见的处理方向”是否有价值；如果 v6 让人声发闷，后续再回收强度。

## 2026-05-26 试听链路诊断版：确认网页播放路径没问题

背景：用户连续反馈 v4/v5/v6 的“听原版 / 听试听版”网页试听完全听不出差异。此时不能继续盲目调降噪参数，因为指标已经显示 preview 与 raw 明显不同（raw RMS/mean/peak 与 preview RMS/mean/peak 差异很大），但实听仍无差异。优先怀疑对象应转为网页按钮、缓存、audio 元素、preview 版本校验或浏览器实际播放源。

本次改动：在 `cloud-voice-server/server.js` 加了临时诊断算法常量 `smooth-play-preview-diagnostic-tone`。试听版仍然只写 `/opt/cardputer-voice/previews/*.play-preview.wav`，不覆盖原始 WAV。生成逻辑在试听版开头加入约 0.2 秒、880Hz 的轻提示音，并把整条试听版额外处理成明显“电话音”效果（诊断用强高通/低通），让 raw 与 preview 的差异必须一耳朵能听出来。前端主试听区仍只保留两个大按钮：`听原版` 和 `听试听版`。

同时修了一个容易误导诊断的点：前端自动加载 preview、后端 `/api/jobs/:id/preview/audio` 的版本校验以前硬编码只认 `smooth-play-preview-v6`。如果只改生成算法名而不改这些判断，新 preview 可能被当作旧版本拒绝或不自动加载。现在统一用 `PLAY_PREVIEW_ALGORITHM` / `currentPreviewAlgorithm`，避免版本标记与播放链路脱节。

部署记录：本地 `node --check cloud-voice-server/server.js` 通过；服务器备份为 `/opt/cardputer-voice/server.js.bak-20260526-preview-diagnostic-tone`；上传到 `/opt/cardputer-voice/server.js` 后服务器端 `node --check /opt/cardputer-voice/server.js` 通过；`systemctl restart cardputer-voice.service` 后服务为 `active`。已对 `20260526_101946_cardputer-001_REC_0026` 重新生成 preview，meta 显示：
- `algorithm=smooth-play-preview-diagnostic-tone`
- `diagnosticToneHz=880`
- `diagnosticToneMs=200`
- `diagnosticPhoneHighpass=0.86`
- `diagnosticPhoneLowpass=26`

踩坑：在 Windows PowerShell 里通过 `ssh` 远程执行带 JSON、引号、`$UPLOAD_TOKEN`、`node -e` 的命令很容易被本地 PowerShell 和远端 bash 双重解析，出现过 `unexpected EOF`、token 被拆成多个 curl 参数、`node -e` 的括号被远端 shell 吃掉等问题。最终用更稳的办法：远端 Node 脚本直接读取 `/etc/cardputer-voice.env` 中的 `UPLOAD_TOKEN`，通过本机 `http://127.0.0.1:3000/api/jobs/.../preview` 调用接口生成 preview；对于远程校验 meta，优先用简单 `grep -E 'algorithm|diagnosticTone|diagnosticPhone|createdAt'`，减少跨 shell 引号复杂度。

结论：用户强制刷新后台后，确认这版能听出区别。这证明“听原版 / 听试听版”两个按钮和浏览器播放链路总体是通的，不是一直播放同一个文件，也不是 preview 文件完全没生效。下一步如果要做正式可用的试听算法，应从这个诊断版回退掉提示音和极端电话音，只保留对听感真正有帮助的处理；但不要再把“听不出差异”直接归因于算法参数太轻。若未来又出现“完全听不出差异”，先检查 preview meta 算法名、`/preview/audio` 是否 409、浏览器是否强刷、按钮是否加载的是最新 object URL，再谈参数。

## 2026-05-26 四按钮直听版：原版 / 轻度 / 重度 / 强力

用户希望不要再先选模式、再生成、再加载，而是主试听区直接有四个按钮，点哪个就播放哪个。因此后台主试听区已改为四个大按钮：`听原版`、`轻度清理`、`重度清理`、`强力清理`。清理版如果还没有对应文件，会在点击时自动生成并播放；如果已经生成，则直接加载播放。

为了避免三档互相覆盖，preview 文件改为按 mode 分开保存：`heavy` 仍沿用旧兼容路径 `*.play-preview.wav/json`；`light` 使用 `*.play-preview.light.wav/json`；`strong` 使用 `*.play-preview.strong.wav/json`。接口也支持 `POST /api/jobs/:id/preview` 时带 `{ mode: "light|heavy|strong", params }`，以及 `GET /api/jobs/:id/preview/audio?mode=light|heavy|strong`。算法标记改为 `smooth-play-preview-four-button-v7`。

同时撤掉上一版诊断用的 0.2 秒提示音和极端电话音处理。诊断版已经证明播放链路没问题；四按钮版要回到正式可比较的听感清理，不再故意加可识别标记。REC_0026 已预生成三档：
- `20260526_101946_cardputer-001_REC_0026.play-preview.light.wav`
- `20260526_101946_cardputer-001_REC_0026.play-preview.wav`（重度）
- `20260526_101946_cardputer-001_REC_0026.play-preview.strong.wav`

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-four-listen-buttons`；`cardputer-voice.service` 重启后为 `active`。
## 2026-05-26 四档命名和预加载

用户确认主试听区不需要“轻度清理/重度清理/强力清理”这种带语义负担的词，改成最直观的四档：`原版`、`一档`、`二档`、`三档`。含义固定为：原版 raw；一档=最轻处理；二档=默认/中等处理；三档=最强处理。后续不要再把主按钮文案改复杂，复杂解释只放高级调参区或备忘里。

为什么之前点击后会“加载”：浏览器里的 audio 元素一开始没有拿到对应 WAV 的 blob；如果该档 preview 还没生成，还要先 POST 生成，再 GET 音频。因此第一次点击某档可能会等。现在改成选中录音后自动预加载四档：先加载原版，再并行加载 `light/heavy/strong` 三档；如果某档缺文件，会后台生成并加载。预加载完成后，点击四个按钮应尽量做到同位置快速切换。

实现记录：主按钮文案改为 `原版 / 一档 / 二档 / 三档`；`previewListenModes` 的显示 label 改为 `一档/二档/三档`；`selectJob()` 不再只加载 raw 和二档，而是触发 `preloadListenAudio()`。部署备份：`/opt/cardputer-voice/server.js.bak-20260526-four-level-preload`，本地和服务器 `node --check` 通过，服务重启后为 `active`。

## 2026-05-26 四档试听小 bug 修复：常亮、误切换、四档无差异

用户反馈三个问题：1）播着播着像是自动切换；2）有一个档位一直亮；3）原版、一档、二档、三档听起来仍然没区别。结论是这次不是继续盲目调参数，而是先修播放状态和预加载链路，再把四档处理真正拉开。

原因和修复：
- “二档一直亮”是前端按钮写死了 `class="listen-button primary"`，它只是视觉主按钮样式，不代表播放中，但用户看到就会以为二档一直选中。已移除这个 `primary`，现在只有 `.playing` 才会变绿，播放结束、暂停或出错都会自动取消高亮。
- “播着播着自动切换”主要来自预加载竞态：选中录音后后台会预加载 raw/light/heavy/strong；如果用户已经开始播放，某一路预加载完成后 `loadAudioTo()` 会 `revokeAudio()` 并替换 audio `src`，浏览器会中断当前音频，听起来像被自动切走。已修改为：如果目标 audio 正在播放，预加载不再替换它的 `src`。同时新增 `activeListenAudioId`，同位置切换只跟当前正在播放的音频走，不再拿其他暂停音频的旧 `currentTime` 参与判断。
- “四档没区别”是 v7 虽然有三套参数，但后端最后一层人声带宽整形仍固定为 `voiceBandMix=0.58`、`bodyLowpass=84`，不同档位被同一层尾端滤波压得太接近。已升级为 `smooth-play-preview-four-button-v8`，新增 `previewModeProfile()`：一档 `voiceBandMix=0.92/bodyLowpass=112` 更接近原声；二档 `0.58/84` 中等；三档 `0.28/54` 且 `extraLowpass=42`，会明显更窄带、更电话音，便于确认差异。

部署记录：本地 `node --check cloud-voice-server/server.js` 通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-four-level-bugfix-v8`；上传后服务器 `node --check /opt/cardputer-voice/server.js` 通过；`systemctl restart cardputer-voice.service` 后服务为 `active`。

REC_0026 已重新生成三档 v8 preview：
- `light smooth-play-preview-four-button-v8 light 0.92 112 0 11947564`
- `heavy smooth-play-preview-four-button-v8 heavy 0.58 84 0 11947564`
- `strong smooth-play-preview-four-button-v8 strong 0.28 54 42 11947564`

用户验证时需要强制刷新后台页面，再点 `原版 / 一档 / 二档 / 三档`。如果仍说完全没区别，下一步优先查浏览器是否真的加载了新 HTML 和新 blob URL，而不是再调音频参数。

## 2026-05-26 四档试听 v9：按听感反馈回收三档沉闷，并加强二档差异

用户听 v8 后反馈：`原版 / 一档 / 二档` 基本没区别；`三档` 的底噪基本没有了，但人声明显沉闷，音量也偏小。判断：v8 的三档证明降噪方向有效，但 `extraLowpass=42`、`voiceBandMix=0.28` 太狠，把人声亮度和响度也压掉；二档又太保守，没拉开差异。

v9 调整：
- 算法标记升级为 `smooth-play-preview-four-button-v9`。
- 一档仍保持轻处理，只稍微抬高降噪可感知度：`gain=1.02`、`rumbleHighpass=0.965`、`noiseRmsMax=520`、`noiseMix=0.78`，尾端 profile 为 `voiceBandMix=0.96/bodyLowpass=128/finalGain=1.03`。
- 二档改成真正中档：`gain=1.08`、`rumbleHighpass=0.93`、`noiseRmsMax=900`、`noiseMix=0.52`，尾端 profile 为 `voiceBandMix=0.84/bodyLowpass=112/finalGain=1.08`。
- 三档保留较强底噪处理，但去掉额外低通，避免电话音过闷；同时补偿音量：`gain=1.22`、`rumbleHighpass=0.9`、`noiseRmsMax=1600`、`noiseMix=0.38`，尾端 profile 为 `voiceBandMix=0.72/bodyLowpass=104/extraLowpass=0/finalGain=1.18`。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-four-level-v9-feedback`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v9 preview：
- `light smooth-play-preview-four-button-v9 light 0.96 128 0`
- `heavy smooth-play-preview-four-button-v9 heavy 0.84 112 0`
- `strong smooth-play-preview-four-button-v9 strong 0.72 104 0`

下一步听感目标：二档应该开始能听出底噪被压，但人声不能闷；三档应该仍明显更干净，但比 v8 更亮、更大声。如果三档底噪又回来太多，再优先加强 `noiseRmsMax/noiseMix`，不要再用很低的全局 `extraLowpass` 把整段人声压闷。

## 2026-05-26 四档试听 v10：保人声清晰度优先

用户听 v9 后反馈：只有三档有区别；三档降噪有效，但没有 v8 那么干净，仍有底噪，而且人声变模糊。判断：v8 的强全局带宽整形能压底噪但会闷；v9 回收后仍有轻微模糊，说明不应该继续靠全局低通/带宽收窄来“降噪”，应把主力放到安静段底噪衰减和高通去低频上，同时尽量保留辅音、高频和人声边缘。

v10 调整：
- 算法标记升级为 `smooth-play-preview-four-button-v10`。
- 一档：轻处理但比原版略可感知，`gain=1.04`、`rumbleHighpass=0.95`、`noiseRmsMax=800`、`noiseMix=0.65`，尾端 profile `voiceBandMix=0.98/bodyLowpass=150/finalGain=1.04`。
- 二档：改成更明显的中档，`gain=1.12`、`rumbleHighpass=0.91`、`noiseRmsMax=1500`、`noiseMix=0.35`，尾端 profile `voiceBandMix=0.97/bodyLowpass=150/finalGain=1.10`。
- 三档：继续强降底噪，但几乎取消全局变闷整形，保留清晰度；`gain=1.28`、`rumbleHighpass=0.88`、`lowpass=62`、`highMix=0.90`、`noiseRmsMax=2400`、`noiseMix=0.22`，尾端 profile `voiceBandMix=0.96/bodyLowpass=150/extraLowpass=0/finalGain=1.18`。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-four-level-v10-clarity`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v10 preview：
- `light smooth-play-preview-four-button-v10 light 0.98 150 0`
- `heavy smooth-play-preview-four-button-v10 heavy 0.97 150 0`
- `strong smooth-play-preview-four-button-v10 strong 0.96 150 0`

下一步听感判断：如果三档仍然糊，说明模糊主要来自当前摩擦/底噪判定后对高频的处理，应继续提高 `highMix` 或降低摩擦命中范围；如果三档清晰但底噪太多，优先提高 `noiseRmsMax` 或降低 `noiseMix`，不要恢复 v8 那种 `extraLowpass`。

## 2026-05-26 四档试听 v11：使用 REC_0045 作为机器底噪参考

用户提供 `20260526_120803_cardputer-001_REC_0045`，说明这是“蒙在被子里”的录音。分析结果显示：这条样本时长约 12.69s，原始 `mean≈4962`、`RMS≈4975`，去掉直流偏置后 AC RMS 约 356，能量主要集中在 20-160Hz。这说明此前听到的底噪主要不是环境声，而是机器录音链路自身的直流/低频底座和底噪。

v11 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v11-machine-noise`。
- 新增 `MACHINE_NOISE_REFERENCE_ID`，默认 `20260526_120803_cardputer-001_REC_0045`；生成 preview 时会读取 `/uploads/<id>.wav` 作为机器底噪参考。参考文件缺失时不报错，只退回普通处理。
- 新增 `analyzeMachineNoiseReference()`，对参考样本做同参数高通分析，记录 `machineNoiseRmsP50/P90`，并据此生成 `machineAdaptiveNoiseRms`。
- 新增低频底座抑制：用 `humLowpass/humMix` 跟踪并扣除残留低频底座，而不是用 `extraLowpass` 或强人声带宽收窄把整段人声压闷。
- 三档 profile：一档 `humMix=0.18/voiceBandMix=0.995`；二档 `humMix=0.38/voiceBandMix=0.99`；三档 `humMix=0.55/voiceBandMix=0.985`。目标是减少机器低频底噪，同时尽量保留人声清晰度。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v11-machine-noise`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v11 preview，meta 确认引用了机器底噪样本：
- `light smooth-play-preview-four-button-v11-machine-noise ... machineNoiseReferenceId=20260526_120803_cardputer-001_REC_0045 humMix=0.18`
- `heavy smooth-play-preview-four-button-v11-machine-noise ... machineNoiseReferenceId=20260526_120803_cardputer-001_REC_0045 humMix=0.38`
- `strong smooth-play-preview-four-button-v11-machine-noise ... machineNoiseReferenceId=20260526_120803_cardputer-001_REC_0045 humMix=0.55`

下一步听感判断：如果 v11 三档比 v10 清晰且底噪下降，说明“机器底噪参考 + 低频底座扣除”方向正确。若底噪仍明显但人声清晰，继续加强 `machineNoiseScale/noiseMix/humMix`；若人声仍糊，优先减小 `humMix` 或缩小噪声门限，别再用全局低通。

## 2026-05-26 四档试听 v12：人声出声保护

用户听 v11 后反馈：底噪已经降低但仍有；人声整体清晰，但出声时会带一点模糊声。判断：v11 的机器底噪参考方向正确，但处理在“有人声帧”里仍然太用力，尤其是 hum 扣除和底噪衰减会跟随人声边缘，造成出声瞬间轻微糊。

v12 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v12-speech-protect`。
- 新增 `speechLike` 帧判断：基于机器噪声样本的 `avgDiffP90` 和当前帧 RMS 判断当前帧是否像人声。
- 新增 `speechProtectedFrames` 指标，确认有多少帧触发了人声保护。
- 人声帧里自动降低处理强度：一档 `speechHumRatio=0.35/speechNoiseRatio=0.35`；二档 `0.28/0.22`；三档 `0.22/0.12`。安静段仍按原强度压机器底噪。
- 安静段处理比 v11 稍强：三档 `humMix=0.68`、`machineNoiseScale=8.8`，但人声帧只使用约 22% 的 hum 扣除强度，避免说话时变糊。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v12-speech-protect`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v12 preview：
- `light smooth-play-preview-four-button-v12-speech-protect speechProtectedFrames=19687 processedFrames=13137`
- `heavy smooth-play-preview-four-button-v12-speech-protect speechProtectedFrames=19703 processedFrames=19676`
- `strong smooth-play-preview-four-button-v12-speech-protect speechProtectedFrames=19556 processedFrames=21987`

下一步听感判断：如果 v12 的出声模糊减轻而底噪仍可接受，就沿这个方向继续微调；如果底噪仍多，只加强安静段参数，不加强人声帧参数。若人声仍糊，继续降低 `speechHumRatio/speechNoiseRatio` 或收紧 `speechLike` 判定。

## 2026-05-26 四档试听 v13：降低出声瞬间模糊和尖锐感

用户听 v12 后反馈：底噪更低，但人声出声瞬间的模糊仍在，而且人声有一点尖锐。判断：v12 的人声保护方向有效但还不够，问题主要有两点：1）hum 低频底座跟踪器在人声帧里仍会被人声牵动，造成出声瞬态糊；2）三档高通/增益/高频保留偏激，导致声音发尖。

v13 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v13-speech-soft`。
- 新增 `speechHumTrackRatio`：人声帧里不只是降低 hum 扣除量，还把 hum 跟踪速度降到一档 12%、二档 7%、三档 4%，相当于人声期间近似冻结低频底座跟踪器，避免它追着人声跑。
- 三档回收尖锐度：`gain` 从 1.30 降到 1.22，`rumbleHighpass` 从 0.84 回到 0.89，`highMix` 从 0.96 降到 0.88，`finalGain` 从 1.18 降到 1.12。
- 安静段仍保持较强底噪处理：三档 `machineNoiseScale=9.6/humMix=0.78/noiseMix=0.15`，但人声帧只使用 `speechHumRatio=0.06/speechNoiseRatio=0.06`。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v13-speech-soft`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v13 preview：
- `light smooth-play-preview-four-button-v13-speech-soft speechHumTrackRatio=0.12`
- `heavy smooth-play-preview-four-button-v13-speech-soft speechHumTrackRatio=0.07`
- `strong smooth-play-preview-four-button-v13-speech-soft speechHumTrackRatio=0.04`

下一步听感判断：如果出声瞬间模糊减轻且尖锐感下降，v13 可作为当前网页试听基线；如果底噪仍能接受但人声还糊，继续降低人声帧处理比例；如果人声好了但底噪回来了，只加强安静段参数，不再动人声帧。

## 2026-05-26 四档试听 v14：进一步减少人声帧处理

用户听 v13 后反馈：出声瞬间模糊少了，尖锐感也降了一点，但仍然存在。判断：方向继续正确，但人声帧仍有残余处理痕迹；因此 v14 不再加大全局降噪，而是进一步降低人声帧处理比例，同时把三档高频和增益继续回收。

v14 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v14-speech-clean`。
- 人声帧处理再降：三档 `speechHumRatio=0.025`、`speechHumTrackRatio=0.012`、`speechNoiseRatio=0.03`，几乎只在安静段强压底噪。
- 三档降低尖锐感：`gain=1.16`、`rumbleHighpass=0.91`、`highMix=0.82`、`finalGain=1.08`，比 v13 更软。
- 安静段继续增强：三档 `humMix=0.88`、`machineNoiseScale=10.8`、`noiseRmsMax=3200`、`noiseMix=0.13`。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v14-speech-clean`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v14 preview：
- `light smooth-play-preview-four-button-v14-speech-clean speechHumTrackRatio=0.06`
- `heavy smooth-play-preview-four-button-v14-speech-clean speechHumTrackRatio=0.025`
- `strong smooth-play-preview-four-button-v14-speech-clean speechHumTrackRatio=0.012`

下一步听感判断：如果 v14 的人声更自然但底噪略多，可接受的话先把 v14 作为网页试听基线；如果仍糊，说明剩余糊感可能不来自 hum 跟踪，而来自 noise gate 包络或录音原始链路，需要考虑固件端降低录音增益/DC blocker，而不是继续在服务器试听里硬压。

## 2026-05-26 四档试听 v15：人声期间保留固定底噪压制

用户听 v14 后发现：说话的时候底噪会回来。判断：v14 为了减少模糊，几乎关闭了人声帧内的噪声处理，导致机器底噪在说话期间重新露出。v15 的目标是：人声期间仍不让 hum 跟踪器追人声，但保留一部分“固定底噪压制”。

v15 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v15-speech-bed`。
- 保持人声期间低 hum 跟踪：三档 `speechHumTrackRatio=0.008`、`speechHumRatio=0.02`，避免出声瞬间糊。
- 提高人声期间的固定底噪压制：三档 `speechNoiseRatio=0.16`，二档 `0.20`，一档 `0.28`，让说话时底噪不要完全回来。
- 新增 `speechBodyProtect`，人声帧内对较强的人声主体提高保护，三档 `speechBodyProtect=1.75`，避免新的 speech noise 抑制造成人声被压糊。
- 三档继续维持更柔和的人声参数：`gain=1.12`、`rumbleHighpass=0.91`、`highMix=0.80`、`finalGain=1.06`。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v15-speech-bed`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v15 preview：
- `light smooth-play-preview-four-button-v15-speech-bed speechNoiseRatio=0.28 speechBodyProtect=1.35`
- `heavy smooth-play-preview-four-button-v15-speech-bed speechNoiseRatio=0.20 speechBodyProtect=1.55`
- `strong smooth-play-preview-four-button-v15-speech-bed speechNoiseRatio=0.16 speechBodyProtect=1.75`

下一步听感判断：如果说话时底噪少了但模糊又回来，说明服务端时域门控已接近上限，下一步应转向更合适的“频谱噪声指纹/谱减”或固件端降低录音增益/DC blocker。不要继续靠更狠的时域 gate 硬压。

## 2026-05-26 四档试听 v16：频谱机器噪声指纹

用户反馈：不说话时底噪能压下去，但说话时底噪会回来，并追问能否得到纯净人声。判断：时域 gate/hum 扣除只能在“不说话”或低能量段有效；当人声和机器底噪同时存在时，普通门控如果继续压就会糊人声，如果保护人声就会让底噪回来。要处理“说话时叠在人声下面的稳定底噪”，必须改用频谱噪声指纹/谱减，而不是继续加大时域 gate。

v16 改动：
- 算法标记升级为 `smooth-play-preview-four-button-v16-spectral-profile`。
- 新增纯 JS STFT/FFT 谱减链路：用 `REC_0045` 生成平均机器噪声频谱，对输出 preview 做频谱减法。
- 三档频谱强度：一档 `spectralStrength=0.28/spectralFloor=0.22`；二档 `0.65/0.16`；三档 `1.05/0.10`。`spectralFloor` 用来避免过度谱减产生严重水声或金属声。
- 保留 v15 的时域低频底座处理和人声保护，频谱减噪作为最后一层，用于处理“说话时仍在的人声下方底噪”。

部署记录：本地和服务器 `node --check` 均通过；服务器备份 `/opt/cardputer-voice/server.js.bak-20260526-v16-spectral-profile`；`cardputer-voice.service` 重启后为 `active`。REC_0026 已重新生成三档 v16 preview，生成耗时约 6 秒/档：
- `light smooth-play-preview-four-button-v16-spectral-profile spectralStrength=0.28 spectralFloor=0.22`
- `heavy smooth-play-preview-four-button-v16-spectral-profile spectralStrength=0.65 spectralFloor=0.16`
- `strong smooth-play-preview-four-button-v16-spectral-profile spectralStrength=1.05 spectralFloor=0.10`

结论：完全“纯净人声”不现实，尤其是单麦克风、16k、机器底噪已经录进人声同频段时；能做的是在“底噪残留”和“人声自然度/水声伪影”之间选平衡。v16 是目前第一版真正针对说话期间底噪的方案。如果三档出现水声/金属感，降低 `spectralStrength` 或提高 `spectralFloor`；如果仍有底噪但人声自然，逐步提高谱减强度。若想从根上减少底噪，下一步应改固件端录音增益、DC blocker 和模拟/电源链路，而不是只靠服务器后处理。

> 最后更新: 2026-05-26。给自己和 AI 看的速查手册。

---
## 🔖 续接说明(开新窗口先看这段)

**文档地图**:
- 总入口：`项目备忘.md`（本文件）。
- 文档目录：`docs/README.md`。
- 踩坑日志：`docs/TROUBLESHOOTING_LOG.md`（避免重复踩同一个坑）。
- 固件说明：`docs/firmware/HANDOVER.md`、`docs/firmware/COMPILE_FLASH.md`、`docs/firmware/DESIGN_SPEC.md`。
- 服务器说明：`docs/server/README.md`、`docs/server/DEPLOYMENT.md`、`docs/server/SERVER_MEMO.md`。
- 历史方案/旧页面：`docs/archive/`。
- 配置工具页仍在：`toolbox/NET_CONFIG_HELPER.html`。

**2026-05-25 本轮结束状态（最重要）**:
- 最新固件已经成功烧录到 Cardputer-Adv（COM3），使用 `esp32:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=default_8MB`；本轮写入成功输出包含 ESP32-S3、Embedded Flash 8MB、`Hash of data verified`、`Hard resetting with RTC WDT`。
- 录音/播放实时链路已调到当前稳定体验版：录音写 SD 采用小批量写入并优先重启麦克风采样；录音和播放的 A/B 波形保持约 60fps 局部刷新；底部时间数字、暂停/继续、开始录音、长按删除进度都改成局部刷新，不再整屏闪。
- 上传改成更安全的“协作式后台队列”：列表页按 `\` 只入队，不当场阻塞；空闲时自动慢慢传；用户按键会暂停当前尝试但保留 `UP` 队列；录音/播放期间不跑上传，避免 SD/Wi-Fi 抢实时音频资源。
- 经验要点：录音连续性优先级最高，显示可以局部快刷但不能整屏频繁 `pushSprite`；任何 Wi-Fi、NTP、HTTP、SD 大读写都不能进入录音/播放实时内圈。相关踩坑记录见 `docs/TROUBLESHOOTING_LOG.md` 的 2026-05-25 实时链路条目。
- 云端服务器已部署并重启成功，`https://cardputer.flye.cc/health` 正常；最近提交已推送 GitHub：`aff7a7e Add adaptive ADPCM uploads`、`0df7c03 Document troubleshooting log`。
- 上传后台已可用：`https://cardputer.flye.cc/dashboard`。输入 `UPLOAD_TOKEN` 后可看服务健康、正在上传、上传速度/剩余时间、最近任务、设备 Wi-Fi RSSI/IP、job 详情和音频播放。
- 长录音上传加速已完成：超过 3MB 的 WAV 会在小机器端临时压成 IMA ADPCM 上传，服务器收到后还原成标准 WAV 再转写；短录音仍直接传 WAV。
- 真机验证已通过：超过 3MB 的长录音后台显示 `ADPCM`，上传速度明显提升，并能正常转写和发 flomo；短录音后台显示 `WAV`，不会被压缩，流程正常。
- 后台会显示上传格式 `WAV` / `ADPCM`、压缩后上传大小、原始大小和压缩倍率。长文件如果仍显示 `WAV`，先确认是否低于 3MB 或机器端是否已经写入新固件。
- 后台任务详情页已增强为检查台：可播放原始 WAV，看转写、flomo memo、上传/音频元数据、文件状态，并可重跑转写/重发 flomo。
- 音频实验区第一版已上线：可在详情页调“增益、低通、高频保留、摩擦命中阈值”等参数，生成不覆盖原文件的 `play-preview.wav`，和原始录音 A/B 试听。页面刷新后会保留上次试听参数和处理统计。下一步等用户听感反馈后继续调参。
- 修过一个服务端重试坑：如果服务器残留半截 `uploads/REC_xxxx.wav` 但没有对应 job JSON，新上传会删除 stale partial 并允许重试，避免机器一直 `UP` 而后台无任务。
- 新增总踩坑日志：`docs/TROUBLESHOOTING_LOG.md`。以后遇到串口、黑屏、上传慢、后台 token、dashboard、esp32 core、键盘库等问题，先查这里。

**2026-05-24 稳定基线状态**:
- 固件已成功烧录到 Cardputer-Adv（COM3），使用 `FlashSize=8M,PartitionScheme=default_8MB`。
- 固件已包含“盒子层”联网能力：Wi-Fi、HTTP 上传、上传队列、NTP 校时、`X-Recorded-At` 上传时间字段。
- 端到端已真机跑通：小机器录音 → 按 `\` 上传 → `http://cardputer.flye.cc/upload` → 服务器转写 → flomo 推送成功。
- 录音上传不是自动上传全部文件；用户必须按 `\`：
  - 录音中按 `\`：结束当前录音并把这一条加入上传队列。
  - 录音列表选中一条后按 `\`：只上传选中的这一条。
- 小机器端 `Wi-Fi` 应用已写入代码并成功烧录，现场输入/修改 Wi-Fi 名和密码后会写回 `SD:/UPLOAD/net.txt` 并测试连接。
- 小机器不负责输入长 token。复杂服务器配置在电脑填写：
  - 打开 `toolbox/NET_CONFIG_HELPER.html`
  - 填服务器 `url`、上传 `token`、设备名（Wi-Fi 可电脑填，也可之后在小机器上改）
  - 生成 `net.txt`
  - 放到 SD 卡 `SD:/UPLOAD/net.txt`
- 小机器上传状态提示：`UP` = 已加入上传队列/等待或重试上传；`SV` = 小机器已成功把 WAV 上传到服务器(server received)。注意：`SV` 不代表小机器确认 flomo 已完成，flomo 成功由服务器 jobs 状态负责。
- 上传防卡顿已收紧：后台每次只试 1 条，Wi-Fi/HTTP 等待更短；当前新版按键会暂停本次尝试并保留队列，避免列表页长时间像“死机”，也避免误把用户操作理解成取消上传。
- 云端后台页面入口：`http://cardputer.flye.cc/dashboard`。打开后输入 `UPLOAD_TOKEN`，可看服务健康、正在上传进度、最近 jobs、设备最近一次上传带来的 Wi-Fi RSSI/IP。
- 其他提示：`NO CFG` = `net.txt` 缺失或配置不完整（例如 `ssid` 为空）；`WIFI ERR` = Wi-Fi 失败；`BAD URL` = 地址格式错；`HTTP ERR` = 服务器响应失败。
- 下一轮建议：先做可靠性和可观测性，不急着加新功能。服务器侧加 `/jobs` 最近列表、失败重试、避免 flomo 重复发送；设备侧如需更细状态，可通过查询 `/jobs/REC_XXXX` 显示 flomo 完成状态。

**2026-05-24 服务器/flomo 窗口结束状态（同样重要）**:
- 阿里云 ECS 已部署 `cloud-voice-server` 到 `/opt/cardputer-voice`，由 `cardputer-voice.service` 托管，nginx 只为专用域名/端口转发，不接管现有站点。
- 正式上传入口已经可用：`http://cardputer.flye.cc/upload`；健康检查：`http://cardputer.flye.cc/health`；job 查询：`http://cardputer.flye.cc/jobs/REC_XXXX`。
- DNS 已生效：`cardputer.flye.cc -> 47.110.91.244`。
- 服务器已配置 `UPLOAD_TOKEN`、`ASR_FILE_TOKEN`、`DASHSCOPE_API_KEY`、`FLOMO_WEBHOOK_URL`，只在服务器 `/etc/cardputer-voice.env`，不要写进 GitHub/备忘文档。
- 上传链路已端到端验证：`REC_ASR_0001.wav` 上传后自动走 DashScope Paraformer 转写，生成 `transcripts/REC_ASR_0001.txt/json`，并成功写入 flomo；测试转写文本为 `12345。上山打老虎。`，job 状态为 `done`。
- 服务器端代码与部署模板已推送 GitHub：`2c8d05d Add DashScope transcription and flomo sync`。本地部署说明见 `docs/server/DEPLOYMENT.md`。
- 给机器端生成 `net.txt` 时，`url` 用 `http://cardputer.flye.cc/upload`；`token` 从服务器窗口/服务器环境变量取得，不要提交。

**整机已基本完成且稳定**:开机→空格录音(绿色声波+W/S调音量)→停止→回车播放→N降噪。录音文件在电脑上听是干净的。

**⚠️ 当前有一处刚改、等用户验证(最重要)**:
为消除"开机爆音",最新一版把 `cfg.internal_spk = false`(开机不启用扬声器),并**手动配置扬声器**(setup 里设引脚 41/43/42、I2S_NUM_1)+ **playWav 里手动写 ES8311 DAC 寄存器**(0x00=80,01=B5,02=18,0D=01,12=00,13=10,32=BF,37=08)开启 DAC。
- **要确认两点**:① 冷启动开机不再爆音;② **按回车播放是否还有声音**(手动配 DAC 若有误会变没声音)。
- **若播放没声音了**:把 `internal_spk` 改回默认(删掉 `cfg.internal_spk=false` 和手动 speaker.config、删掉 playWav 里手动 DAC 那段、并在 setup 预热末尾恢复 `Speaker.begin()`),即回到"有声音但开机会爆音"的稳定版。仓库 `toolbox/toolbox.ino` 是当前(internal_spk=false)版本。

**音频状态机(当前设计, 为了少爆音)**:
- 开机: 不开扬声器(无开机爆音), 麦克风预热一次(空跑录音会话, 修冷启动首录音量小/断续)。
- 录音: 只用麦克风。**停止录音不关麦**(避免停止爆音), 麦保持开着。
- 播放(playWav): 这时才 `Mic.end()` + `Speaker.begin()` + 手动开 DAC → 播放 → 不关扬声器。
- 仍存在的小问题: **播放前一下"功放上电爆音"**(最难消, 紧挨声音、较自然, 暂留)。

**爆音问题进展**: 停止录音爆音=已修(停止不关麦); 设备播放"哒哒"=已修(播放改双缓冲, 之前单缓冲被覆盖); 开机爆音=最新版尝试用 internal_spk=false 消除(待验证)。

**2026-05-24 录音音质/按键摩擦处理结论**:
- 当前可用版本保留 GitHub 基线实时录音链路: `REC_RATE=16000`, `REC_N=256`, `recGain=50`, `mc.magnification=1`, `mc.noise_filter_level=3`, `processMicBuffer()` 仍是软件增益 + `lpf` 系数 `160` + `tanhf` 软限幅。
- 新增保存后轻量按键摩擦处理 `suppressKeyFriction(path, effData)`: 录完写好 WAV 后扫描一遍 PCM, 只处理"低 RMS + 高相邻采样变化量"的帧。
- 样本来源: `REC_0399.wav`=纯按键/手指摩擦, `REC_0400.wav`=说话夹杂摩擦。分析结论: 摩擦声 RMS 低(A线不明显), 但高频比例/相邻采样变化量高; 人声帧 RMS 高、频谱重心更低。
- 当前规则: `rms < 1900 && avgDiff > 140 && avgDiff*256 > rms*90` 或 `rms < 2200 && avgDiff > 300` 判为摩擦; 命中后 `hold=2` 个缓冲。
- 当前处理方式: 不压整块音量, 只削高频刮擦分量。每帧做一阶低通 `lp += (sample-lp)*96/256`, 分离 `hi=sample-lp`, 输出 `lp + hi*64/256`。这样比旧方案自然, 不会把说话变成断断续续。
- 明确踩坑: 不要再用强实时 AGC/强语音门/长时间整块衰减处理摩擦或碰撞; 这些会让人声变糊、断续, 且按键摩擦不一定会被 A线/RMS 捕捉到。

**2026-05-25 录音处理新方向：人耳播放版和机器识别版分开**:
- 以后不要把“小机器播放出来好听”和“阿里云语音识别更准”混成一个目标。它们是两套处理。
- 小机器端目标：录得稳、播放舒服、少刺耳、少刮擦、不要爆音。算法要轻，不能占太多资源，也不要让录音链路变脆。
- 服务器端目标：给语音转文字用。可以不好听，但要让人声更清楚、字头字尾别被切掉、识别更准。
- 原始 WAV 永远保留。推荐流程：`raw.wav` 原始录音 → 小机器/电脑预览用“人耳版处理” → 服务器生成 `clean_for_asr.wav` 给 DashScope → 保存转写和 flomo memo。
- 小机器算法可以拆成单独文件，比如 `audio_process.h/.cpp`，把实时轻处理、按键摩擦处理、参数结构从 `toolbox.ino` 里分出来。
- 电脑网页优先调“参数”，不要每次推 C++ 源码。调好后通过 Wi-Fi 推一份配置到小机器 SD 卡，例如 `/UPLOAD/audio.cfg` 或 `/CONFIG/audio.json`。大改算法时再重新编译烧录。
- 网页端要能 A/B 对比：原始录音、小机器播放版、服务器识别版。用户能听、能看波形、能看到哪些地方被判为刮擦声。
- 服务器处理不能是黑箱。网页后台要显示每条录音的处理步骤、使用的参数版本、处理后的文件、阿里云转写结果、最终发到 flomo 的文本。
- 后续可做 AI 自动调参闭环：录音 → 上传 → 网页/服务器分析 → 调参数 → Wi-Fi 推到小机器 → 再录音验证。第一阶段先人工网页调参，稳定后再自动化。

---

## 一、这是什么
给 M5Stack **Cardputer-Adv** 写的一个"工具箱"固件(Arduino/C++)。
- 风格: 纯黑底 + 黑客帝国荧光绿 + 中文界面。
- 主页: `[空格] 录音`  `[回车] 播放最近`。
- 录音机: 实时绿色声波(示波器), 录音中 **W 调大 / S 调小** 音量, **其他键停**。
- 录完结果页: `[回车] 播放`  `[N] 降噪`(FFT 谱减法)  其他键返回。
- 录音存成 16kHz 单声道 WAV, 放在 SD 卡 `/REC/REC_xxxx.wav`。

## 二、文件在哪
| 东西 | 路径 |
|---|---|
| **源代码(改这个)** | `C:\cardputer\sketches\toolbox\toolbox.ino` |
| 代码备份 | `D:\github仓库同步\小机器\toolbox\toolbox.ino` |
| arduino-cli(命令行编译器) | `C:\cardputer\tools\arduino-cli\arduino-cli.exe` |
| 库(M5Cardputer/M5Unified/M5GFX) | `C:\cardputer\Arduino\libraries\` |
| 各种安装/下载脚本 | `C:\cardputer\*.ps1` |

⚠️ **所有路径必须是纯英文**! ESP32 编译器和 PowerShell 读中文路径会出错, 所以工具和代码都放在 `C:\cardputer\`(不要放到带中文的目录里编译)。

## 三、怎么编译 + 烧录(核心流程)
1. 用**能传数据的 USB-C 线**把机器连到电脑(劣质/充电线会"无法识别 USB", 换线!)。
2. 电源开关 ON。
3. 在 PowerShell 里跑(端口一般是 COM3, 不确定先用 `arduino-cli board list` 查):
```
$cli="C:\cardputer\tools\arduino-cli\arduino-cli.exe"
& $cli compile --fqbn esp32:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=default_8MB C:\cardputer\sketches\toolbox
& $cli upload  --fqbn esp32:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=default_8MB -p COM3 C:\cardputer\sketches\toolbox
```
- **开发板型号(FQBN)**: `esp32:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=default_8MB`(8MB Flash + 3MB APP 分区, 给 Wi-Fi 上传留空间)。
- 盒子层联网配置在 SD 卡 `/UPLOAD/net.txt`；除 `ssid/password/url/token/device` 外，时钟可选 `ntp=pool.ntp.org`、`tz=8`。联网成功后盒子层会做 NTP 校时，上传时可带 `X-Recorded-At`。
- 长配置不在小机器上手敲；电脑打开 `toolbox/NET_CONFIG_HELPER.html` 填服务器 URL/token，然后生成/下载 `net.txt` 放到 SD 卡 `/UPLOAD/`。Wi-Fi 名/密码可在小机器端 `Wi-Fi` 应用里现场修改。
- 如果烧不进/认不到: 进**下载模式** = 电源 OFF → 按住 G0 → 通电 → 松开 G0, 再烧。
- 看程序崩溃日志: 用串口工具连 COM3 @115200, 会打印 `Guru Meditation` 等。

## 四、关键的坑和决定(很重要, 别重复踩)
1. **核心必须用 esp32 3.1.3, 不能用 3.3.x!**
   - 3.3.8(最新)上**麦克风录不了音**(录到静音/平线), 这是新核心的 I2S 录音回归 bug。
   - 3.1.3(IDF 5.3)麦克风正常。降级方法见 `C:\cardputer\prefetch313.ps1`。
2. **键盘库被改成"轮询"了**(去掉了 GPIO 中断)。
   - 文件: `...\M5Cardputer\...\KeyboardReader\TCA8418.cpp`。
   - 原因: 原版用 GPIO 中断, 在 3.1.3 上开机会撞爆系统 ipc1 任务栈 → 不停重启/闪屏。
   - ⚠️ **如果重装 M5Cardputer 库, 这个改动会丢, 要重新改**(把 begin() 里的 attachInterrupt 删掉、update() 改成 `while(available()) getEvent...` 轮询)。
3. **开机会"预热"麦克风**(空跑一次录音会话 + 暖一下 SD), 否则**冷启动后第一次录音音量小、断续**。
4. **国内下载慢**: esp32 核心用 Jihulab 镜像 + 从 `dl.espressif.cn` 用 curl 高速预取到缓存, 再让 arduino-cli 从缓存装(arduino-cli 自带下载器对国内 CDN 很慢)。
5. **这颗芯片(ESP32-S3FN8)没有 PSRAM** → 跑不了 Espressif ESP-SR 专业降噪。所以只能用自写的轻量谱减法降噪。
6. 喇叭是 1W 小喇叭, 本身就小声; **要大声清晰用 3.5mm 耳机**。

## 五、可调参数(都在 toolbox.ino 里)
| 参数 | 位置 | 作用 |
|---|---|---|
| `recGain`(默认 50) | 文件顶部 | 录音音量(也可录音时 W/S 现场调) |
| 低通系数 `160` | recorderApp 的 lpf 那行 | 越小越闷、压高频底噪越多 |
| `HEAD_SKIP`(13) | recorderApp | 掐头时长(去开始键声) |
| `tailTrim`(REC_RATE/4) | 收尾处 | 掐尾时长(去结束键声) |
| 软限幅 `tanhf` | 每采样处理 | 防大声"哒哒"爆裂 |
| `suppressKeyFriction()` | 保存后处理 | 按键/手指摩擦抑制: 低 RMS + 高 avgDiff 帧只削高频, 不整块降音量 |
| 降噪 `ALPHA`(3.0)/`BETA`(0.05) | noiseReduce() | 谱减强度/谱底; ALPHA 越大降噪越狠但易有"水声" |
| 喇叭 DAC `0x32` | playWav() | 回放音量微调 |

## 六、当前状态 / 待办
- ✅ 开机不崩不闪、空格录音、绿色波形、回车回放、冷启动第一次正常、W/S 调音量、掐头去尾。
- 🔧 调优中: 大声"哒哒"(软限幅只去了一部分)、底噪/降噪强度。
- 💡 想要真·干净录音: 把 SD 卡插电脑, 用 Audacity 一键降噪(效果远超机器端)。
- 🧊 以后可加: 更多工具(红外遥控/笔记/计时器)、把工具箱导出成 .bin 放进 M5Launcher 菜单。
## 2026-05-26 固件端机器底噪前处理 v1

背景：服务器端 v16 频谱机器噪声指纹已经把网页试听里的底噪压得很干净，但它依赖 FFT/STFT 和参考噪声谱，不适合直接搬进 Cardputer 的实时录音内圈。机器本体没有 PSRAM，录音循环还要同时服务 SD 写入和 UI 刷新，所以固件端先做轻量源头治理，而不是把服务器算法硬塞进去。

本次固件改动只影响 `toolbox/toolbox.ino` 的实时录音链路，不覆盖已有 WAV，不烧录机器。改动点：
- 默认 `recGain` 从 `50` 降到 `36`，减少把机器本底噪声一起放大的概率；录音中仍可用 `W/S` 现场调大或调小。
- `processMicBuffer()` 新增 `recHum` 慢速低频底座跟踪器：每帧先做 DC/high-pass，再追踪机器自己的低频/偏置残留，并从样本里扣掉一部分。
- 静音或非人声段更积极扣低频底座；检测到类似人声时，追踪变慢、扣除比例降低，避免把字头和人声主体一起削掉。
- 没有使用 FFT、神经网络或重型降噪库；这是固件端“先少录进一点底噪”的保守方案，真正干净的人声仍主要依赖服务器端 v16 这种离线处理。

验证记录：直接用普通 `esp32:esp32:m5stack_cardputer` 分区编译会失败，因为程序 `1753348 bytes` 超过默认 `1310720 bytes` 上限；这不是本次改动导致的语法问题，而是该项目一直需要 8MB Flash + 3MB APP 分区。使用正式 FQBN `esp32:esp32:m5stack_cardputer:FlashSize=8M,PartitionScheme=default_8MB` 后编译通过：程序 `1753396 bytes`，占 `52%`；全局变量 `138200 bytes`，占 `42%`。

后续验证方式：目前只编译通过，尚未烧录。烧录后建议重新录一条“蒙在被子里”的静音样本和一条正常说话样本，对比服务器后台的原档/三档。若原始 WAV 本身底噪明显下降，说明源头治理有效；若声音偏小，优先用录音时 `W` 把 `recGain` 加回去，而不是先改服务器参数。
## 2026-05-26 新窗口首次编译固定入口

问题复盘：之前每次新开 Codex 窗口，首次编译容易先踩几次坑，并不是固件代码错，而是命令没有固定下来：
- 直接在仓库中文路径下指定 build 输出，ESP32 链接器有时无法写出 `.elf`。
- 使用默认 `esp32:esp32:m5stack_cardputer` 会走 1.31MB 默认分区，当前程序必然超限；必须使用 `FlashSize=8M,PartitionScheme=default_8MB`。
- 沙箱内直接跑 Arduino CLI 可能无法写 `C:\Users\87194\AppData\Local\Arduino15`，会误报 `Access is denied` 或 `Platform 'esp32:esp32' not found`。
- 多次超时重跑会留下并行编译进程，反而更慢。

已新增固定脚本：`tools/cardputer_build.ps1`。以后新窗口第一条命令直接用它：

```powershell
# 只编译
powershell -ExecutionPolicy Bypass -File "D:\github仓库同步\小机器\tools\cardputer_build.ps1"

# 编译并烧录，通常端口是 COM3
powershell -ExecutionPolicy Bypass -File "D:\github仓库同步\小机器\tools\cardputer_build.ps1" -Flash -Port COM3
```

脚本会自动把 `toolbox/toolbox.ino` 同步到 `C:\cardputer\sketches\toolbox\toolbox.ino`，创建 `C:\cardputer\build_fresh\out`，用固定 FQBN 和 `--jobs 1` 编译；加 `-Flash` 时再用同一个 build 输出烧录。若 Codex 需要权限，直接批准这条脚本即可，不要先尝试默认编译命令。
